NODE_ENV=development
PORT=4000

# Neon (Postgres)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Auth (opcional, usa valor padrão se não definido)
JWT_SECRET=troque-isto-por-um-segredo-grande-com-mais-de-16
# ADMIN_PASSWORD não é mais necessário (usa tabela users)
