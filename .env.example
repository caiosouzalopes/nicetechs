# Senha do painel admin (opcional; padrão: Nct0103@)
# ADMIN_PASSWORD=sua_senha_segura

# Para o estoque ser o MESMO em todos os navegadores e dispositivos, use JSONBin (grátis):
# 1. Crie conta em https://jsonbin.io
# 2. Crie um "bin" com conteúdo: {"products":[]}
# 3. Copie a API Key (API Keys no site) e o ID do bin (na URL do bin)
# JSONBIN_API_KEY=sua_api_key_aqui
# JSONBIN_BIN_ID=id_do_seu_bin

# Se não configurar JSONBIN_*, o estoque continua funcionando por dispositivo (localStorage).
# Com JSONBIN configurado, qualquer alteração no admin sincroniza para todos.
