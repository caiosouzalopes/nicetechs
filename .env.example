NODE_ENV=development

# Neon (Postgres)
# Use a connection string do Neon (Vercel → Storage → Neon / ou Neon console)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# JWT Secret (opcional, usa valor padrão se não definido)
JWT_SECRET=troque-isto-por-um-segredo-grande-com-mais-de-16
